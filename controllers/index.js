const company = require('./company')
const branch = require('./branch')
const user = require('./user')
const profile = require('./profile')
const role = require('./role')

module.exports = { company, branch, profile, role, user };